<%- include('partials/header') %>

<main>

    <h1>
        <%= title %>
    </h1>

    <p>Browse our available service project categories:</p>

    <ul>

        <% categories.forEach(category => { %>

            <li>
                <strong>
                    <%= category.name %>
                </strong>
            </li>

        <% }) %>

    </ul>

</main>

<%- include('partials/footer') %>
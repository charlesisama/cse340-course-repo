<%- include('partials/header') %>

    <main>

        <h1>
            <%= title %>
        </h1>
        <p class="new-project"><a href="/new-project">Add a New Service Project</a></p>

        <div class="card-grid">
        
            <% projects.forEach(project=> { %>
        
                <article class="card">
        
                    <h2>
                        <%= project.title %>
                    </h2>
        
                    <p>
                        <strong>Location:</strong>
                        <%= project.location %>
                    </p>
        
                    <p>
                        <strong>Date:</strong>
                        <%= project.project_date %>
                    </p>
        
                    <a class="card-btn" href="/project/<%= project.project_id %>">
                        View Project
                    </a>
        
                </article>
        
                <% }) %>
        
        </div>

    </main>

    <%- include('partials/footer') %>